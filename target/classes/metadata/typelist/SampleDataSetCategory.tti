<?xml version="1.0"?>
<typelist xmlns="http://guidewire.com/typelists"
          desc="Categories of sample data (non-production)"
          final="false"
          subpackage="system"
          name="SampleDataSetCategory">
    <typecode code="additive" desc="Sample data that builds on smaller data sets.  Typekey priority is used to determine what to load." name="Additive" priority="0"/>
    <typecode code="standalone" desc="Sample data that is not meant to build on other data sets.  Priority is ignored except for display purposes." name="Standalone" priority="4"/>
</typelist>
