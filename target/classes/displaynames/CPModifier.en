<?xml version="1.0"?>
<Entity
  name="CPModifier">
  <Columns>
    <Column
      beanPath="CPModifier.Pattern.Name"
      name="Name"
      sortOrder="1"/>
  </Columns>
  <DisplayName><![CDATA[// CPModifier is a modifier on the line, so it is not state specific
return Name]]></DisplayName>
  <DisplayName
    type="Validation"><![CDATA[return displaykey.EntityName.Modifier.Validation(Name)
]]></DisplayName>
</Entity>